# 文档归档说明

当前仓库已进入测试、联调与验收阶段。

为减少顶层 `docs/` 的噪音，历史或过程性材料统一归档到这里：

- `delivery/`：客户发包、汇报、FAQ、打包建议等包装材料
- `process/`：实施基线、前端方案、推进进展、冻结清单、阶段总览等开发过程材料
- `stages/`：阶段性范围、执行清单、样例请求、联调请求等里程碑材料

顶层 `docs/` 仅保留当前测试、验收、联调直接使用的核心文档。
